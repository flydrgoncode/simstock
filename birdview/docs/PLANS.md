# Birdview Next Phase Plan

## Goal
- Create the foundations for Workspace, Droplet Studio, the unified command language, the droplet shadow skill schema, and the dummy-data preview runner.

## Phase 1
- Add route shells for `Workspace` and `Droplet Studio`.
- Add module folder foundations for `workspace`, `droplet-studio`, `command-language`, and `dummy-data`.
- Add shared schema folders for commands, droplets, and dummy-data.
- Add a unified command parser stub with strict typed results.
- Build a `Workspace` 3-column shell.
- Build a `Droplet Studio` shell.

## Later Phases
- Expand the command language beyond the stub parser.
- Implement the full droplet shadow skill schema workflow.
- Add the dummy-data preview runner execution flow.
- Wire the new foundations into Home Control and Runtime behaviors.
